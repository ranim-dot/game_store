spring:
  datasource:
    url: jdbc:mysql://localhost:3306/game_store
    username: root
    password: Rootranim@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        globally_quoted_identifiers: true
